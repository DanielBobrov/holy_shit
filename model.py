# Копируем содержимое существующего файла model.py
import torch
import torch.nn as nn
import torch.nn.functional as F
import math
import json
import os
import matplotlib.pyplot as plt
import seaborn as sns
from typing import List, Dict, Tuple, Optional, Union
from torch.cuda.amp import autocast

# ... существующий код ...
# Т.к. этот файл уже существует, предполагаю, что нет необходимости дублировать его содержимое.
